プロンプトに「Hello, World!」と表示するコードを作成してください。
入力値:無し
出力値:Hello, World!

入力された数字(n)に対して以下条件に従い文言を表示するコードを作成してください。
条件:
    nが奇数の場合、規格外と表示
    nが偶数で、2~5の場合、規格内と表示
    nが偶数で、6~20の場合、規格外と表示
    nが偶数で、20以上の場合、規格内と表示
入力値:整数n　(1<= n <= 100)
出力値: 「規格内」もしくは「規格外」の文字

入力された数字（a,b）に対して次の3行を出力してください。
1行目 入力された数字の和
2行目 入力された数字の差（一つ目の数字-二つ目の数字）
3行目 入力された数字の積
入力値（例）:3と2
出力値（例）:
5
1
6

入力された数字（a,b）に対して次の2行を出力してください。
1行目 入力された数字の商（小数点以下切り捨て）
2行目 入力された数字の商（小数点込み）

入力された数字（n）に対して、0~n-1の数字すべてを2乗した数字を1行ずつ出力してください。
入力値(例): 3
出力値(例):
0
1
4

4年ごとにカレンダーに1日が追加され、その日はうるう日と呼ばれます。 
それは地球が太陽を周回するのに約365.25日かかるためにカレンダーを修正する必要があります。 

グレゴリオ暦では、うるう年を識別するために3つの条件が使用されます。

・年は4で均等に分割できます。ただし、次の場合を除き、うるう年です。
　・年は100で均等に分割できますが、次の場合を除いてうるう年ではありません。
　　・年も400で均等に割り切れます。それからうるう年です。

つまり、グレゴリオ暦では、2000年と2400年はうるう年ですが、1800、1900、2100、2200、2300、2500はうるう年ではありません。

指定された年がうる年かどうかを判定する関数を作成してください。
なお関数名は「is_leap」
引数名は「year」
戻り値はBoolean型の「True」もしくは「False」とします。
leap = year % 4 == 0 and (year % 400 == 0 or year % 100 != 0)

入力された数字（n）に対して、(文字列を足すなどの)文字列メソッドを使用せずに、以下を出力してみてください。
入力値: n
出力値: 123...n
例　入力 = 5
    出力 = 12345

とあるテストの集計を作業を任されました。テストの結果から2番目に高い点数を抽出してください。
入力値1: 総回答数を表す数字(n)
入力値2: 各回答の点数がスペース区切りされたもの(A[] of n)
出力値: 2番目に大きい数字

例
　入力1 = 5
　入力2 = 2 3 6 6 5
　出力 = 5

入力された名前の順番をシャッフルするアルゴリズムを書いてください。

入力値: スペースで区切られた名前
出力値: 順番がシャッフルされた名前

例
    入力: セラク 太郎
    出力: 太郎 セラク